/**
 * =====================================================
 * CONFIGURAÇÃO DOS PIXELS DE RASTREAMENTO
 * =====================================================
 * 
 * Edite os IDs abaixo para configurar seus pixels.
 * Depois de editar, salve o arquivo e atualize a página.
 */

const PIXEL_CONFIG = {
    // =====================================================
    // KWAI PIXEL
    // =====================================================
    KWAI: {
        ENABLED: true,                    // true = ativo, false = desativado
        PIXEL_ID: '297940027162507'       // Seu ID do pixel do Kwai
    },

    // =====================================================
    // FACEBOOK PIXEL (Meta)
    // =====================================================
    FACEBOOK: {
        ENABLED: true,                    // true = ativo, false = desativado
        PIXEL_ID: 'SEU_PIXEL_ID_AQUI'     // Substitua pelo seu ID do pixel do Facebook
    }
};

// NÃO EDITE ABAIXO DESTA LINHA
// =====================================================
